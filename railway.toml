[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"
watchPatterns = [
    "requirements.txt", 
    "photobiz/"
    "templates/**", 
    "users/**", 
    "payments/**", 
    "downloads/**", 
    "railway.toml",
    "Dockerfile",
]

